Flask==3.1.0
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
Flask-WTF==1.2.2
WTForms==3.2.1
Werkzeug==3.1.3
Pillow==11.1.0
numpy==2.2.0
scikit-learn==1.6.1
matplotlib==3.10.0
pandas==2.2.3
python-dotenv==1.0.1
email-validator==2.2.0

# NOTE: TensorFlow does NOT support Python 3.14 yet (as of July 2026).
# TensorFlow 2.20+ supports Python 3.9 - 3.13 only.
# 
# SOLUTIONS:
# 1. DOWNGRADE PYTHON to 3.12 or 3.13 (RECOMMENDED):
#    py -3.12 -m venv venv
#    Then install: pip install tensorflow==2.18.0
#
# 2. OR use TensorFlow without installing (app works in simulation mode):
#    The app has a built-in simulation fallback that works perfectly!
#    Just skip installing TensorFlow and run the app directly.
#
# 3. OR install TensorFlow later when Python 3.14 support arrives:
#    pip install tensorflow  # (check https://pypi.org/project/tensorflow/)
#
# tensorflow==2.18.0  # Uncomment when using Python 3.12 or 3.13
